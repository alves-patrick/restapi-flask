apiVersion: v1
kind: Service
metadata:
  name: {{ include "restapi-flask.fullname" . }}
  labels:
    {{- include "restapi-flask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.service.port | default 80 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "restapi-flask.fullname" . }}
